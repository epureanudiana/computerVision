im1 = imread('sphere1.ppm');
im2 = imread('sphere2.ppm');

figure(2);
subplot(211);
imshow(im1);
hold on;
[v_x, v_y] = lucas_kanade(im1,im2);
title("Sphere");

subplot(211);
imshow(im1);
hold on;
[v_x, v_y] = lucas_kanade(im1,im2);
title("Sphere");